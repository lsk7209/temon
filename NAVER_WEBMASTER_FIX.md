# 네이버 웹마스터도구 문제 해결 가이드

## 발견된 문제

1. **리다이렉션된 페이지**: 7개
2. **동일한 제목인 웹문서**: 10개
3. **동일한 설명문**: 10개

## 해결 방법

### 1. 리다이렉션 문제 해결 ✅

**문제**: `app/test/[slug]/page.tsx`가 클라이언트 사이드 리다이렉트를 사용

**해결**:
- 서버 사이드 리다이렉트로 변경 (`redirect()` 함수 사용)
- `next.config.mjs`에 레거시 경로 리다이렉트 추가

**변경 사항**:
- ✅ `app/test/[slug]/page.tsx` - 서버 사이드 리다이렉트로 변경
- ✅ `next.config.mjs` - 레거시 경로 리다이렉트 추가

### 2. 중복 제목 문제 해결 🔄

**문제**: 많은 페이지가 "| 16유형 분석 | 테몬" 같은 패턴 사용

**해결**: 각 페이지마다 고유한 제목 생성

**패턴**:
```tsx
// ❌ 이전 (중복)
title: "백업 습관 테스트 | 16유형 분석 | 테몬"

// ✅ 개선 (고유)
title: "백업 습관 테스트 - 스마트폰 데이터 백업으로 알아보는 성격 유형 | 테몬"
```

**적용 방법**:
```tsx
import { generateUniqueTestMetadata } from "@/lib/quiz-seo-utils"

export const metadata: Metadata = generateUniqueTestMetadata({
  testName: "테스트 이름",
  testCategory: "테스트 카테고리 (예: 스마트폰 데이터 백업)",
  testDescription: "테스트 설명 (80자 이하 권장)",
  keywords: "키워드1, 키워드2, 키워드3",
  canonical: "/tests/test-id",
})
```

### 3. 중복 설명 문제 해결 🔄

**문제**: 많은 페이지가 비슷한 설명 사용

**해결**: 각 페이지마다 고유한 설명 생성

**패턴**:
```tsx
// ❌ 이전 (중복)
description: "스마트폰 데이터를 백업하는 습관으로 알아보는 나의 성격 유형. 12문항, 결과 공유 이미지 자동 생성."

// ✅ 개선 (고유)
description: "스마트폰 데이터를 백업하는 습관으로 알아보는 나의 성격 유형. 사진을 백업할 때, 데이터를 잃어버렸을 때 등 구체적인 상황으로 분석합니다."
```

## 수정된 파일

### 리다이렉션
- ✅ `app/test/[slug]/page.tsx`
- ✅ `next.config.mjs`

### 중복 제목/설명
- ✅ `app/tests/phone-backup/page.tsx`
- ✅ `app/tests/phone-payment/page.tsx`
- ✅ `app/tests/phone-search/page.tsx`
- 🔄 나머지 페이지들도 같은 패턴으로 수정 필요

## 나머지 페이지 수정 가이드

### 단계별 가이드

1. **파일 열기**: `app/tests/{test-id}/page.tsx`

2. **메타데이터 수정**:
```tsx
// 이전
export const metadata: Metadata = {
  title: "테스트 이름 | 16유형 분석 | 테몬",
  description: "공통 설명...",
  // ...
}

// 이후
import { generateUniqueTestMetadata } from "@/lib/quiz-seo-utils"

export const metadata: Metadata = generateUniqueTestMetadata({
  testName: "테스트 이름",
  testCategory: "고유한 카테고리 설명",
  testDescription: "고유한 테스트 설명 (80자 이하)",
  keywords: "키워드1, 키워드2",
  canonical: "/tests/test-id",
})
```

3. **고유성 확보**:
   - `testName`: 테스트의 정확한 이름
   - `testCategory`: 다른 테스트와 구별되는 카테고리
   - `testDescription`: 해당 테스트만의 고유한 설명

### 예시

#### phone-message
```tsx
generateUniqueTestMetadata({
  testName: "메시지 습관 테스트",
  testCategory: "스마트폰 메시지",
  testDescription: "스마트폰으로 메시지를 보내는 습관으로 알아보는 나의 성격 유형. 메시지를 보낼 때, 답장을 할 때 등 구체적인 상황으로 분석합니다.",
  keywords: "메시지, 스마트폰, 메시지 습관, 디지털 테스트, 성격 테스트",
  canonical: "/tests/phone-message",
})
```

#### phone-social-media
```tsx
generateUniqueTestMetadata({
  testName: "소셜미디어 습관 테스트",
  testCategory: "소셜미디어 사용",
  testDescription: "소셜미디어를 사용하는 습관으로 알아보는 나의 성격 유형. 게시물을 올릴 때, 댓글을 달 때 등 구체적인 상황으로 분석합니다.",
  keywords: "소셜미디어, SNS, 소셜미디어 습관, 디지털 테스트, 성격 테스트",
  canonical: "/tests/phone-social-media",
})
```

## 검증 방법

1. **네이버 웹마스터도구**:
   - 유형별 진단 정보 확인
   - 리다이렉션된 페이지: 0개 목표
   - 동일한 제목: 0개 목표
   - 동일한 설명문: 0개 목표

2. **로컬 테스트**:
   ```bash
   # 빌드 테스트
   npm run build
   
   # 린트 확인
   npm run lint
   ```

## 완료 체크리스트

- [x] 리다이렉션 문제 해결
- [ ] 중복 제목 문제 해결 (진행 중)
- [ ] 중복 설명 문제 해결 (진행 중)
- [ ] 모든 페이지에 고유한 메타데이터 적용
- [ ] 네이버 웹마스터도구 재검증

---

**마지막 업데이트**: 2025-01-XX
**상태**: 🔄 진행 중

