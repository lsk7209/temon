# 확장성 최적화 가이드

## 📋 목적
100개, 300개의 테스트를 효율적으로 관리하고 추가할 수 있도록 코드와 폴더 구조를 최적화했습니다.

---

## 🏗️ 최적화된 구조

### 1. 공통 컴포넌트 추출 ✅

#### `components/test-question-page.tsx`
- 모든 테스트 질문 페이지의 공통 로직을 하나의 컴포넌트로 추출
- 상태 관리, Analytics 추적, 자동 이동 로직 등 모든 기능 포함
- 각 테스트는 설정만 전달하면 됨

**Before (각 테스트마다 200+ 줄 중복)**
```typescript
// app/tests/coffee-mbti/test/page.tsx
export default function CoffeeMBTITest() {
  const [currentQuestion, setCurrentQuestion] = useState(0)
  const [answers, setAnswers] = useState<Record<number, string>>({})
  // ... 200줄 이상의 중복 코드
}
```

**After (설정만 전달)**
```typescript
// app/tests/coffee-mbti/test/page.tsx
import { TestQuestionPage } from "@/components/test-question-page"
import { getTestConfigSync } from "@/lib/tests/registry"

export default function CoffeeMBTITest() {
  const config = getTestConfigSync("coffee-mbti")
  return <TestQuestionPage config={config} />
}
```

### 2. 질문 데이터 JSON 분리 ✅

#### `lib/tests/questions/{test-id}.json`
- 각 테스트의 질문 데이터를 JSON 파일로 분리
- 질문 추가/수정이 코드 변경 없이 가능
- 콘텐츠 관리가 용이

**구조:**
```json
{
  "testId": "coffee-mbti",
  "questions": [
    {
      "id": 1,
      "text": "질문 텍스트",
      "choices": [
        { "id": "a", "text": "선택지 A", "tags": ["E"] },
        { "id": "b", "text": "선택지 B", "tags": ["I"] }
      ]
    }
  ],
  "calculateResult": "mbti",
  "resultPath": "/tests/coffee-mbti/test/result",
  "colorTheme": { ... }
}
```

### 3. 테스트 레지스트리 시스템 ✅

#### `lib/tests/registry.ts`
- 모든 테스트 설정을 중앙 관리
- 동적 로딩 지원 (필요시)
- 결과 계산 함수 자동 매핑

**지원하는 결과 계산 방식:**
- `mbti`: MBTI 결과 계산
- `score`: 점수 기반 결과 계산
- `average`: 평균 점수 계산

### 4. 유틸리티 함수 추출 ✅

#### `lib/test-utils.ts`
- 공통 계산 로직을 함수로 추출
- `calculateMBTIResult`: MBTI 결과 계산
- `calculateScoreBasedResult`: 점수 기반 결과 계산
- `calculateAverageScore`: 평균 점수 계산

### 5. 테스트 생성 스크립트 ✅

#### `scripts/create-test.ts`
- 새로운 테스트를 자동으로 생성하는 스크립트
- 필요한 모든 파일과 폴더를 자동 생성
- 템플릿 기반으로 일관된 구조 보장

**사용법:**
```bash
npm run create-test my-new-test "내 새로운 테스트"
```

---

## 📁 최적화된 폴더 구조

```
app/
  tests/
    {test-id}/
      page.tsx              # 인트로 페이지 (간단한 설명)
      test/
        page.tsx            # 질문 페이지 (공통 컴포넌트 사용)
        result/
          page.tsx           # 결과 페이지

lib/
  tests/
    questions/
      {test-id}.json        # 질문 데이터 (JSON)
    registry.ts             # 테스트 레지스트리

components/
  test-question-page.tsx    # 공통 질문 페이지 컴포넌트

scripts/
  create-test.ts            # 테스트 생성 스크립트
```

---

## 🚀 새로운 테스트 추가 방법

### 방법 1: 스크립트 사용 (권장)

```bash
# 1. 테스트 생성
npm run create-test my-test "내 테스트"

# 2. 질문 추가/수정
# lib/tests/questions/my-test.json 편집

# 3. 테스트 정보 추가
# lib/tests-config.ts에 테스트 정보 추가

# 4. 결과 페이지 커스터마이징
# app/tests/my-test/test/result/page.tsx 편집
```

### 방법 2: 수동 생성

1. **질문 JSON 파일 생성**
   - `lib/tests/questions/{test-id}.json` 생성
   - 질문과 선택지 작성

2. **페이지 파일 생성**
   - `app/tests/{test-id}/page.tsx` (인트로)
   - `app/tests/{test-id}/test/page.tsx` (질문 - 공통 컴포넌트 사용)
   - `app/tests/{test-id}/test/result/page.tsx` (결과)

3. **레지스트리 등록**
   - `lib/tests/registry.ts`에 테스트 추가 (필요시)

4. **설정 파일 업데이트**
   - `lib/tests-config.ts`에 테스트 정보 추가

---

## 📊 최적화 효과

### Before (현재 구조)
- 각 테스트: ~250줄
- 100개 테스트: ~25,000줄
- 300개 테스트: ~75,000줄
- 질문 수정 시 코드 변경 필요
- 중복 코드 많음

### After (최적화된 구조)
- 각 테스트 질문 페이지: ~10줄
- 100개 테스트: ~1,000줄 (질문 페이지)
- 300개 테스트: ~3,000줄 (질문 페이지)
- 질문 수정 시 JSON만 수정
- 중복 코드 제거

### 코드 감소율
- **질문 페이지**: 약 95% 감소
- **전체 코드**: 약 80% 감소 (100개 기준)
- **유지보수성**: 크게 향상

---

## ✅ 체크리스트

새로운 테스트를 추가할 때:

- [x] 공통 컴포넌트 사용 (`TestQuestionPage`)
- [x] 질문 데이터는 JSON 파일로 분리
- [x] 테스트 생성 스크립트 활용
- [x] `lib/tests-config.ts`에 테스트 정보 추가
- [x] 결과 계산 방식 확인 (`mbti`, `score`, `average`)
- [x] 색상 테마 설정
- [x] 결과 페이지 커스터마이징

---

## 🎯 다음 단계

### 추가 최적화 가능 항목

1. **동적 라우팅**
   - `app/tests/[testId]/test/page.tsx`로 통합
   - 완전히 동적 라우팅으로 변경

2. **결과 페이지 템플릿**
   - 공통 결과 페이지 컴포넌트 추출
   - 결과 데이터를 JSON으로 관리

3. **CMS 연동**
   - 질문 데이터를 CMS에서 관리
   - 실시간 업데이트 가능

4. **테스트 버전 관리**
   - 질문 변경 이력 추적
   - A/B 테스트 지원

---

**작성일**: 2024년 12월  
**상태**: ✅ 확장성 최적화 완료

