# Status | 마지막: 2026-08-26

## 현재 작업
애드센스 결과 페이지 수익화 — **2회 라이브 시도 모두 실패, 현재 보류**. 결과 페이지는 광고 없이 그대로 유지 중.

## 최근 변경 (최근 5개만)
- 08-26: Auto Ads URL 제외(`temon.kr/tests/*/test/result/*`, 이 섹션의 모든 페이지) 설정 후 슬롯 재개 → 라이브 검증했지만 **여전히 광고 7개, 효과 없음** → 즉시 재롤백. Google 문서상 이 기능은 접두사 매칭만 지원(와일드카드 미지원 가능성 높음)해서 슬러그가 중간에 끼는 우리 URL 구조에선 애초에 작동 불가능한 것으로 결론.
- 08-26: Next.js 14→16, drizzle-orm 0.29→0.45 업그레이드 조사 리포트 2건(Codex 병렬 워커) — 조사만, 코드 무변경.
- 08-25: 결과 페이지 광고 슬롯 1차 라이브 시도 → 광고 7개+vignette 확인 → 롤백.
- 08-25: 레거시 정적 결과 페이지 212개(트래픽 94%)에도 같은 광고 유닛 확장 코드 추가.
- 08-25: `hono` 미사용 의존성 제거 + `npm audit fix`로 취약점 18→10건.

## TODO
- [ ] **결과 페이지 광고는 재시도 전 구조적 결정 필요** — 아래 "결정사항" 참고, 셋 중 하나 고르기 전엔 3번째 라이브 시도 하지 말 것.
- [ ] (착수 시 별도 요청) Next.js 16 / drizzle-orm 0.45 업그레이드 — 리포트만 완료, 실행은 보류.

## 결정사항
- **결과 페이지 Auto Ads 차단은 AdSense 콘솔 URL 제외로 불가능** — 접두사 매칭만 지원하고 슬러그가 URL 중간에 있어 패턴 표현이 안 됨(2026-08-26 라이브 재검증으로 확정).
- 남은 선택지 3가지(docs/HANDOFF.md에 상세):
  1. 결과 URL을 `/results/{slug}` 같은 고정 접두사로 재구조화(코드 작업 필요, 미착수)
  2. 레거시 212개만 "이 페이지만" 정확 일치로 하나씩 등록(DB 기반은 resultId가 매번 달라 커버 불가, 노가다)
  3. Auto Ads 전체 끄고 전 페이지 수동 유닛으로 전환(대규모 작업)
  4. **결과 페이지는 광고 없이 유지(현재 상태, 기본값)**
- 결과 페이지는 `robots: noindex`가 의도적(세션별 resultId URL 중복 방지)이라 광고 게이트에서 indexable 체크는 제외했음(이 결정 자체는 유효, 광고 온오프와 무관).
- 로컬 가짜 슬롯 ID 테스트로는 Auto Ads 문제를 못 잡음 — 반드시 실제 슬롯 라이브 테스트 필요하다는 게 2번 다 확인됨.

## 주의
- `.bkit`, `.omc`, `.omx`, `local.db` 변경은 로컬 상태/cache로 보고 작업 대상에서 제외.
- Vercel CLI가 `lsk7209` 계정으로 로컬에 로그인되어 있어 `vercel env`/`vercel --prod`로 직접 배포 가능.
- 결과 페이지 광고를 또 시도하려면 먼저 사용자와 위 4개 선택지 중 하나를 확정할 것 — 같은 방식 재시도 금지.
