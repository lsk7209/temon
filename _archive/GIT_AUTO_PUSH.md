# Git 자동 업로드 가이드

개발 완료 후 변경사항을 자동으로 GitHub에 업로드하는 방법입니다.

## 🚀 사용 방법

### 1. 대화형 모드 (커밋 메시지 입력)
```bash
npm run git-push
```
- 커밋 메시지를 직접 입력할 수 있습니다
- 변경된 파일을 확인하고 커밋 메시지를 입력하면 자동으로 푸시됩니다

### 2. 자동 모드 (타임스탬프 자동 생성)
```bash
npm run git-push-auto
```
- 커밋 메시지가 자동으로 생성됩니다 (예: "개발 완료 - 2025. 11. 6. 오후 10:35:12")
- 사용자 입력 없이 자동으로 업로드됩니다

### 3. 빌드 + 자동 업로드
```bash
npm run deploy
```
- 프로젝트를 빌드한 후 자동으로 GitHub에 업로드합니다
- 배포 전에 빌드가 성공하는지 확인합니다

## 📋 동작 과정

1. **변경사항 확인**: `git status`로 변경된 파일 확인
2. **파일 스테이징**: `git add .`로 모든 변경사항 추가
3. **커밋**: 커밋 메시지와 함께 커밋 생성
4. **푸시**: 현재 브랜치로 GitHub에 푸시

## ⚠️ 주의사항

- `.env.local` 파일은 `.gitignore`에 포함되어 있어 업로드되지 않습니다
- 커밋 전에 변경사항을 확인하세요
- 충돌이 발생하면 수동으로 해결해야 합니다

## 🔧 문제 해결

### 푸시 실패 시
```bash
# 원격 저장소 상태 확인
git fetch origin

# 원격과 동기화
git pull origin main

# 다시 시도
npm run git-push
```

### 커밋 취소 (푸시 전)
```bash
git reset --soft HEAD~1
```

