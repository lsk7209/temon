# Core Web Vitals Recheck | 2026-08-25 (mobile, PageSpeed Insights v5)

Previous check: 2026-05-27 (3 months old). Re-checked as part of monetization/SEO review, and as a
pre-ad-unit baseline for `/tests/*/test/result/*`.

| Page | Perf score | LCP (lab) | CLS (lab) | TBT | CrUX field data |
| --- | --- | --- | --- | --- | --- |
| `/` | 80 | 2.3s | 0.256 | 170ms | LCP/FCP/CLS/TTFB all "FAST" (28-day real users) |
| `/tests` | 85 | 2.6s | 0.188 | 150ms | not checked |
| `/tests/{slug}/test/result/{id}` (sample, ad unit not yet live) | 98 | 2.1s | 0 | 120ms | not checked |

## Notes

- Lab-measured CLS on `/`(0.256) and `/tests`(0.188) exceeds the 0.1 target, but CrUX real-user field
  data for `/` reports CLS category "FAST". This is a known lab-vs-field gap (a single synthetic
  Lighthouse run can catch a noisy layout shift, e.g. ad slot fill timing, that real users mostly don't
  hit). Not caused by this session's changes — homepage/`/tests` were not modified. Worth a follow-up
  Lighthouse trace if it recurs, but not blocking for this round.
- The sampled result page is currently pristine (CLS 0, perf 98) because `NEXT_PUBLIC_ADSENSE_RESULT_SLOT_ID`
  is unset in production, so `ResultAdUnit` renders nothing. **Once a real slot ID is set and the ad
  unit goes live, re-run this check on the same URL** to confirm the reserved `min-h-[250px]` wrapper
  keeps CLS low.
- LCP on `/` (2.3s) and `/tests` (2.6s) are borderline against the 2.5s target; not a regression, but
  worth watching if additional ad surface area is added later.
